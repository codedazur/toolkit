{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "dev": {
      "cache": false,
      "persistent": true
    },
    "audit": {},
    "lint": {},
    "test": {},
    "build": {
      "dependsOn": ["audit", "lint", "test", "^build"],
      "outputs": ["dist/**", ".next/**", ".storybook/**"]
    },
    "deploy": {
      "dependsOn": ["^build"],
      "env": ["CDK_DEFAULT_ACCOUNT", "CDK_DEFAULT_REGION", "DOMAIN_NAME"]
    },
    "destroy": {
      "env": ["CDK_DEFAULT_ACCOUNT", "CDK_DEFAULT_REGION"]
    }
  }
}
