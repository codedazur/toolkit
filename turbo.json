{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "lint": {},
    "test": {},
    "audit": {},
    "build": {
      "dependsOn": ["lint", "test", "audit", "^build"],
      "outputs": ["dist/**", ".next/**", ".storybook/**"]
    },
    "release": {
      "dependsOn": ["^build"]
    },
    "deploy": {
      "dependsOn": ["^build"],
      "env": ["CDK_DEFAULT_ACCOUNT", "CDK_DEFAULT_REGION", "DOMAIN_NAME"]
    },
    "destroy": {
      "env": ["CDK_DEFAULT_ACCOUNT", "CDK_DEFAULT_REGION"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
